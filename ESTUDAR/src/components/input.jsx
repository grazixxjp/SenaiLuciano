const Input = ({onChange}) => {
    return (  
        <input type="text" onChange={ onChange} className="w-full rounded-[30px] p-3 outline-none" id="email" />
        
    );
}
 
export default Input;